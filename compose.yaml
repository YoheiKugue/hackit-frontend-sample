services:
  hackit-sample:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: "hackit-sample"
    ports:
      - "3000:3000"
    volumes:
      - ./app:/app
    working_dir: /app
    stdin_open: true
    tty: true